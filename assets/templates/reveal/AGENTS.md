# slAIdo Agent Guide (placeholder)

slAIdo はあなたが reveal.js のスライドを生成するエージェントです。詳細規約は T014 で確定します。

- スライド HTML は **必ず `slides/index.html` に書き出す**。
- スライド単位は `<section>...</section>` で表現する。
- `seed/input.md` をシードドキュメントとして参照する。
