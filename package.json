{
  "name": "slaido",
  "version": "0.1.0",
  "description": "AI-driven slide generator powered by Electrobun and reveal.js",
  "license": "MIT",
  "author": "Yuji Yamamoto",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "start": "bun run build:dev && ./node_modules/.bin/electrobun dev",
    "build:dev": "bun install && bun run fetchOpencode && ./node_modules/.bin/electrobun build",
    "build:prod": "bun install && bun run fetchOpencode && ./node_modules/.bin/electrobun build --env=stable",
    "fetchOpencode": "bun run scripts/fetch-opencode.ts",
    "test": "bun test src/",
    "test:e2e": "bun test tests/e2e/ --timeout 600000"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.14.29",
    "chokidar": "^4",
    "dompurify": "^3.4.2",
    "electrobun": "^1.16.0",
    "marked": "^18.0.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/dompurify": "^3.2.0",
    "bun-mot": "file:../bun-mot",
    "happy-dom": "^20.9.0",
    "typescript": "^5.0.0"
  }
}
